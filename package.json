{
  "name": "freelance-client-manager",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=@fcm/api\" \"npm run dev --workspace=@fcm/web\"",
    "build": "npm run build -ws"
  },
  "dependencies": {
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
